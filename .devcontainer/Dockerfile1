# Utiliser une image de base avec Hugo installé
FROM klakegg/hugo:latest

# Définir le répertoire de travail
WORKDIR /usr/src/app

# Copier le contenu du site Hugo dans le conteneur
COPY . .

# Exposer le port sur lequel le serveur Hugo va écouter
EXPOSE 1313

# Commande par défaut pour lancer le serveur Hugo
CMD ["hugo", "server", "--bind", "0.0.0.0"]





